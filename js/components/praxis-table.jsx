import {h} from 'preact';

function headerCell(schema, toptions, fkey) {
    // TODO: Resume here
    return <th></th>;
}

function PraxisHeader(props) {
    let {schema, options, order} = props;

    return <tr>
        (order.map(f => schema.
        </tr>;

}

export function PraxisTable(props) {
    let {data, schema} = props;

    let header = <PraxisHeader schema={schema}/>;

}



